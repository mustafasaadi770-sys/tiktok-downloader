import telebot
import requests
import time

# --- الإعدادات النهائية المصححة ---
# تم وضع التوكن الجديد والشغال من صورك
TOKEN = '8292214871:AAHJwB6DqRxZ240SDD-Ke3YzIKQbX4YA3TE'
ADMIN_ID = 8286321204 
MY_RIGHTS = "@1.3vv"
bot = telebot.TeleBot(TOKEN)

# تخزين البيانات (ملاحظة: في الخطة المجانية بـ Render ستصفر الإحصائيات عند إعادة التشغيل)
users = set()
download_count = 0

def get_data(url):
    try:
        headers = {
            'User-Agent': 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/119.0.0.0 Safari/537.36'
        }
        api_url = f"https://www.tikwm.com/api/?url={url}"
        res = requests.get(api_url, headers=headers, timeout=15).json()
        if res.get('code') == 0:
            return res['data']
        return None
    except Exception as e:
        print(f"Error fetching data: {e}")
        return None

# --- لوحة التحكم والإذاعة (للأدمن فقط) ---
@bot.message_handler(commands=['admin'])
def admin_panel(message):
    if message.from_user.id == ADMIN_ID:
        msg = f"📊 إحصائيات البوت الحالية:\n\n👥 مستخدمين نشطين: {len(users)}\n📥 تحميلات: {download_count}"
        markup = telebot.types.InlineKeyboardMarkup()
        btn = telebot.types.InlineKeyboardButton("📣 إرسال إذاعة للكل", callback_data="start_broadcast")
        markup.add(btn)
        bot.reply_to(message, msg, reply_markup=markup)
    else:
        bot.reply_to(message, "❌ هذا الأمر مخصص للمطور فقط.")

# --- رسالة الترحيب ---
@bot.message_handler(commands=['start'])
def start(message):
    users.add(message.chat.id)
    first_name = message.from_user.first_name
    welcome = f"أهلاً بك يا {first_name} 👋\n\nأرسل رابط تيك توك أو إنستقرام الآن وسأقوم بالتحميل لك فوراً وبدون علامة مائية!"
    bot.reply_to(message, welcome)

# --- معالجة الروابط ---
@bot.message_handler(func=lambda m: ("tiktok.com" in m.text or "instagram.com" in m.text))
def handle_links(message):
    url = message.text.strip()
    users.add(message.chat.id)
    msg = bot.reply_to(message, "⏳ جاري التحميل... فضلاً انتظر ثواني.")
    
    data = get_data(url)
    if data:
        global download_count
        download_count += 1
        
        markup = telebot.types.InlineKeyboardMarkup()
        btn1 = telebot.types.InlineKeyboardButton("🎬 فيديو", callback_data=f"vid_{url}")
        btn2 = telebot.types.InlineKeyboardButton("🎵 صوت MP3", callback_data=f"aud_{url}")
        markup.add(btn1, btn2)
        
        bot.edit_message_text("✅ تم العثور على المقطع! اختر النوع:", message.chat.id, msg.message_id, reply_markup=markup)
    else:
        bot.edit_message_text("❌ عذراً، لم أستطع جلب الفيديو. تأكد أن الرابط صحيح وحاول مجدداً.", message.chat.id, msg.message_id)

# --- معالجة الأزرار والعمليات ---
@bot.callback_query_handler(func=lambda call: True)
def callback_query(call):
    if call.data.startswith("vid_"):
        url = call.data[4:]
        data = get_data(url)
        if data:
            bot.send_video(call.message.chat.id, data['play'], caption=f"✅ تم التحميل بواسطة: {MY_RIGHTS}")
            
    elif call.data.startswith("aud_"):
        url = call.data[4:]
        data = get_data(url)
        if data:
            bot.send_audio(call.message.chat.id, data['music'], caption=f"🎵 {MY_RIGHTS}")

    elif call.data == "start_broadcast":
        if call.from_user.id == ADMIN_ID:
            msg = bot.send_message(call.message.chat.id, "أرسل الآن نص الرسالة (أو الصورة) التي تريد إذاعتها لجميع المستخدمين:")
            bot.register_next_step_handler(msg, perform_broadcast)

def perform_broadcast(message):
    count = 0
    for user_id in users:
        try:
            bot.copy_message(user_id, message.chat.id, message.message_id)
            count += 1
        except:
            continue
    bot.send_message(message.chat.id, f"✅ تمت الإذاعة بنجاح لـ {count} مستخدم.")

# --- التشغيل ---
if __name__ == "__main__":
    print("🚀 البوت يعمل الآن بالتوكن الصحيح ونظام الإذاعة...")
    bot.infinity_polling(skip_pending=True)
