import telebot
import requests
import os
from threading import Thread
from flask import Flask

# --- 1. تشغيل سيرفر وهمي لإبقاء البوت حياً ---
app = Flask('')

@app.route('/')
def home():
    return "Bot is Live!"

def run():
    app.run(host='0.0.0.0', port=int(os.environ.get('PORT', 5000)))

def keep_alive():
    t = Thread(target=run)
    t.start()

# --- 2. إعدادات البوت ---
TOKEN = '8292214871:AAHSXs7jK95MQQVtQ1Sc4TCSNbMDuuE8h-w'
ADMIN_ID = 8286321204 
MY_RIGHTS = "@1.3vv"

bot = telebot.TeleBot(TOKEN)
users = set()

# دالة جلب بيانات تيك توك
def get_tiktok_data(url):
    try:
        api_url = f"https://www.tikwm.com/api/?url={url}"
        res = requests.get(api_url, timeout=15).json()
        if res.get('code') == 0:
            return res['data']
        return None
    except:
        return None

# دالة جلب بيانات بينترست (جديد)
def get_pinterest_data(url):
    try:
        # استخدام API خارجي لتحليل روابط بينترست
        api_url = f"https://api.boxentriq.com/social/video-downloader/get-video-url?url={url}"
        res = requests.get(api_url, timeout=15).json()
        if res.get('url'):
            return res['url']
        return None
    except:
        return None

# --- 3. الأوامر ---
@bot.message_handler(commands=['start'])
def start(message):
    users.add(message.chat.id)
    bot.reply_to(message, f"أهلاً بك {message.from_user.first_name} 👋\nأرسل رابط تيك توك أو بينترست وسأحمله لك!")

@bot.message_handler(commands=['admin'])
def admin(message):
    if message.from_user.id == ADMIN_ID:
        markup = telebot.types.InlineKeyboardMarkup()
        btn = telebot.types.InlineKeyboardButton("📣 إرسال إذاعة", callback_data="bc")
        markup.add(btn)
        bot.reply_to(message, f"📊 عدد المستخدمين: {len(users)}", reply_markup=markup)

# --- 4. معالجة الروابط ---

# فحص إذا كان الرابط لتيك توك أو بينترست
@bot.message_handler(func=lambda m: any(word in m.text for word in ["tiktok.com", "pinterest.com", "pin.it"]))
def handle_download(message):
    url = message.text.strip()
    wait_msg = bot.reply_to(message, "⏳ جاري المعالجة...")
    
    # إذا كان الرابط بينترست
    if "pinterest.com" in url or "pin.it" in url:
        video_url = get_pinterest_data(url)
        if video_url:
            bot.delete_message(message.chat.id, wait_msg.message_id)
            bot.send_video(message.chat.id, video_url, caption=f"تم التحميل من بينترست ✅\n{MY_RIGHTS}")
        else:
            bot.edit_message_text("❌ عذراً، لم أتمكن من استخراج الفيديو من هذا الرابط.", message.chat.id, wait_msg.message_id)
            
    # إذا كان الرابط تيك توك
    elif "tiktok.com" in url:
        data = get_tiktok_data(url)
        if data:
            markup = telebot.types.InlineKeyboardMarkup()
            markup.add(
                telebot.types.InlineKeyboardButton("🎬 فيديو", callback_data=f"v_{url}"),
                telebot.types.InlineKeyboardButton("🎵 صوت", callback_data=f"a_{url}")
            )
            bot.edit_message_text("✅ اختر النوع:", message.chat.id, wait_msg.message_id, reply_markup=markup)
        else:
            bot.edit_message_text("❌ فشل جلب بيانات تيك توك.", message.chat.id, wait_msg.message_id)

@bot.callback_query_handler(func=lambda call: True)
def callback(call):
    if call.data.startswith("v_"):
        data = get_tiktok_data(call.data[2:])
        if data: bot.send_video(call.message.chat.id, data['play'], caption=MY_RIGHTS)
    elif call.data.startswith("a_"):
        data = get_tiktok_data(call.data[2:])
        if data: bot.send_audio(call.message.chat.id, data['music'], caption=MY_RIGHTS)

# --- 5. التشغيل ---
if __name__ == "__main__":
    keep_alive()
    print("🚀 البوت يعمل الآن ويدعم تيك توك وبينترست...")
    bot.infinity_polling(skip_pending=True)
